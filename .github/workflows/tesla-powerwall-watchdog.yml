name: Tesla Powerwall Schedule Watchdog

# PAUSED 2026-08-29: GitHub schedule on tesla-powerwall.yml and cron-job.org
# are both firing. Re-enable the schedule block below if those stop.
# Manual Run workflow still works.

on:
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  watchdog:
    runs-on: ubuntu-latest
    steps:
      - name: Watchdog paused
        run: |
          echo "Watchdog schedule is paused. Primary tesla-powerwall.yml + cron-job.org own the timer."
          echo "Re-add on.schedule in tesla-powerwall-watchdog.yml if GitHub cron dies again."
